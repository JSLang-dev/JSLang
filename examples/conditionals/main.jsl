function absolute(value: i32): i32 {
    if (value < 0) {
        return -value;
    } else {
        return value;
    }
}

function main(): i32 {
    const shouldCalculate: bool = true;
    const result: i32 = shouldCalculate ? absolute(-42) : 0;
    console.error(result);
    return result;
}
