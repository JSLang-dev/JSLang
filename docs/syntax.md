# Syntax

v0.0.6 parses functions, variables, blocks, returns, conditionals, expressions, calls, imports, and exports. `jsl check` validates the scope, name, and primitive type rules described in [the language specification](language-spec.md). `jsl build` supports the documented `i32` function subset.
